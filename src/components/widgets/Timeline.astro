---
import classnames from 'classnames'
export interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---


<ol class={classnames('relative border-l border-gray-200', className)}>
  <slot />
</ol>
